# 2026-08-04 首版验收记录

## 已通过

- `npm run check`：14 项核心/存档/地图测试通过，TypeScript 与生产构建通过。
- 地图生成：S/M/L 各 100 个 seed；逐层验证尺寸、传送门数量和关键点连通性。
- `npm run test:e2e`：生产包共 7 项通过、1 项按条件跳过；覆盖桌面 Chromium、iPhone 15 尺寸移动 WebKit、PWA 清单与 Service Worker。
- Chromium 首次联网后断网刷新，仍可进入洞府；Playwright WebKit 的离线网络模拟会触发内核错误，因此 iOS 离线冷启动仍保留给真实 iPhone 15 验收。
- 393×852 窄屏人工试玩：洞府 → 选择 S 地图 → 键盘移动 6 步 → 遭遇山狼 → CD 自动施法 → 胜利 → 刷新续玩。
- 浏览器运行日志：无 error / warn。
- 战斗人工回归中发现并修复动画完成回执被渲染更新取消的问题；新增对应动画锁测试。
- 修复 React StrictMode 下 WebKit 重复挂载 Phaser 画布的问题。
- 视觉不沿用参考线框：完成宣纸/松墨/朱砂视觉层、地形纹理、符印角色、CD 圆环和战斗横幅；新增 1200×630 公开分享图。
- GitHub Pages 子路径构建验证通过：`GITHUB_REPOSITORY=CapooFun/ResignToCultivateWeb npm run build`，manifest、Service Worker fallback、脚本、图标和分享图路径均带仓库前缀。

## 仍需外部验收 / 优化

- Capoo 已确认公开站 **可以访问**（https://capoofun.github.io/ResignToCultivateWeb/）。
- 真实 iPhone 15：Safari / 主屏幕 / 安全区 / 后台 / 横竖屏 / 离线冷启动等系统项仍建议逐项勾（见根目录 `开发交接.md`）。
- **当前阶段 = 优化迭代**：玩法骨架不重做；按 Capoo 反馈改体验与内容。
- Vercel：非必须（Pages 已是主发布线）。

GitHub Pages 公开发布：**已完成**（`CapooFun/ResignToCultivateWeb` + Actions）。
