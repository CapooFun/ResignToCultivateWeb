---
title: 《辞职修仙传（网页版）》开发交接
description: 给下一位 AI 的当前工程状态、验证证据与继续开发入口
updated: 2026-08-04
author: Codex（GPT-5）→ Cursor（Grok）续写发布
status: active
---

# 《辞职修仙传（网页版）》开发交接

> 工程路径：`/Users/Capoo/游戏项目/ResignToCultivateWeb`  
> **请在本工程目录用 Cursor 开窗口继续开发**（不要只在 Infans_Vault 窗口里改代码）。

## 先给结论（2026-08-04 晚）

| 项 | 状态 |
|---|---|
| 本地首版 Demo（玩法骨架 / 测试 / 视觉 / PWA） | ✅ 已完成，勿重做 |
| GitHub 仓库 + Pages 发布 | ✅ 已完成 |
| Capoo 确认公开站可访问 | ✅ 已确认 |
| 体验与内容优化 | 🟡 **当前主线**：问题很多，按反馈逐项修 |
| 真实 iPhone 15 系统项验收（主屏幕 / 离线冷启动等） | ⬜ 随优化一并核对，未全部勾完 |

**公开地址**：https://capoofun.github.io/ResignToCultivateWeb/  
**仓库**：https://github.com/CapooFun/ResignToCultivateWeb  
**分支**：`main`（push 即触发 Pages 再部署）

阶段判断：**能玩的公开 Demo 已通；进入「优化迭代」阶段，不是从零实现。**

---

## 协作约定（Capoo 已切换）

- 改代码 / 跑测试 / 推送：在 **本工程 Cursor 窗口** 与 AI 聊。
- Vault 只更新规格决策与里程碑摘要：`/Users/Capoo/Infans_Vault/30_事业顺利/游戏开发/辞职修仙传/`。
- Capoo 会口述「哪里不爽」；AI 先复现或对齐规格，再小步改、测、推 `main`。

---

## 已完成（勿重复开工）

- 技术栈：React + Phaser 3 + TypeScript + Vite + IndexedDB + PWA。
- 规则在纯 TS `GameCore`；Phaser / React 只渲染与派发命令。
- 战斗：连续时间 CD 自动；攻速普攻 + 秘术独立 CD；三丹药快捷手动。
- 地图：S/M/L，16/24/32，1/2/3 层，seed + 连通性。
- 循环：洞府 → 选图 → 探索战斗 → 回府；死亡 → 轮回天赋。
- 洞府：仓库、采矿、炼丹、炼器、装配。
- 存档：IndexedDB 双份、导入导出、清档确认。
- PWA：manifest、SW、离线壳、更新提示、横屏暂停。
- 视觉：宣纸/松墨/朱砂体系（线框仅信息参考）。
- 发布：`CapooFun/ResignToCultivateWeb` + Actions Pages；线上资源 200；Capoo 已能打开。

证据摘要：`npm run check` 14/14；S/M/L 各 100 seed；e2e 7 过 1 跳过（WebKit 离线模拟内核问题）；393×852 窄屏人工闭环。详见 `docs/2026-08-04_首版验收记录.md`。

---

## 当前主线：优化清单（待填 / 待排）

> Capoo 原话：可以访问了，但还有很多需要优化的点。下列先占位，**在工程窗口里按他下一条反馈追加并勾选**。

### A. 实机 / 发布相关

- [ ] iPhone 15 Safari 完整闭环手感（移动、战斗、回府）
- [ ] 添加到主屏幕 · standalone · 无地址栏
- [ ] 刘海 / Home 条：底栏秘术 CD · 血蓝 · 三丹药不被挡
- [ ] 锁屏 / 切后台再回：状态与音频/计时是否正常
- [ ] 横竖屏行为
- [ ] 离线冷启动（e2e WebKit 离线曾跳过，必须以实机为准）

### B. 体验与内容（预期会堆在这里）

- [ ] （待 Capoo 点名：手感 / UI / 数值 / 文案 / 性能 / 存档…）

改完一项：本地验证 → 推 `main` → 等 Pages → 让 Capoo 刷缓存或硬刷新再试。

---

## 关键文件入口

| 文件 | 作用 |
|---|---|
| `src/game/types.ts` | 契约 |
| `src/game/content.ts` | 内容表 |
| `src/game/core.ts` | 规则 reducer |
| `src/game/mapGenerator.ts` | 地图生成 |
| `src/game/save.ts` / `store.ts` | 存档与派发 |
| `src/components/MapView.tsx` | Phaser 地图与战斗反馈 |
| `src/App.tsx` / `styles.css` | UI 与视觉 |
| `tests/*` · `e2e/*` | 回归 |
| `.github/workflows/deploy-pages.yml` | Pages 部署 |

玩法权威规格（只读决策用）：Vault `辞职修仙传/10_设计/玩法设计总纲.md`。

---

## 发布备查

1. 仓库已建并 push；Pages = GitHub Actions。
2. 子路径构建：`GITHUB_REPOSITORY=CapooFun/ResignToCultivateWeb npm run build`
3. 无后端静态 PWA；日后要云存档/排行再评估 Vercel 或后端。
4. Vercel 配置文件可留，当前主发布线是 Pages。

---

## 安全边界

- 不把 Vault 私人数据提交进本仓；不提交 `node_modules/`、`dist/`、`test-results/`、凭据。
- 不删测试、不伪造断言。
- 未完成实机关键项前，不要写「iPhone 15 验收全部通过」。
- push 前确认只含本工程改动。

---

## 给工程窗口 AI 的最短提示

本仓公开 Demo 已通：https://capoofun.github.io/ResignToCultivateWeb/  
Capoo 会在本窗口反馈优化点。先读本文与他的最新消息，**按反馈修，不要重做骨架**。改完测一下再推 `main`。
