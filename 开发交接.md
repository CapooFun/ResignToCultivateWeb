---
title: 《辞职修仙传（网页版）》开发交接
description: 给下一位 AI 的当前工程状态、验证证据与继续开发入口
updated: 2026-08-04
author: Codex（GPT-5）
status: active
---

# 《辞职修仙传（网页版）》开发交接

> 交接人：Codex（GPT-5）  
> 交接日期：2026-08-04  
> 工程路径：`/Users/Capoo/游戏项目/ResignToCultivateWeb`

## 先给结论

本地首版 Demo 已完成并通过核心、地图、生产构建、桌面 Chromium、移动 WebKit 和 PWA 验收。

**GitHub Pages 已发布成功**（2026-08-04）：

- 仓库：https://github.com/CapooFun/ResignToCultivateWeb
- 公开地址：https://capoofun.github.io/ResignToCultivateWeb/
- Actions：`Deploy to GitHub Pages` 成功（build + deploy）
- 线上抽检：`/`、`manifest.webmanifest`、主 JS/CSS、图标、`og.jpg`、`sw.js` 均为 200；manifest / HTML 资源前缀均为 `/ResignToCultivateWeb/`

**仍未完成**：真实 **iPhone 15** Safari / 主屏幕 PWA 验收（需 Capoo 本机；AI 无法代替实机触摸、刘海、锁屏后台与离线冷启动）。

视觉不照搬用户提供的线框图。线框只作为信息关系参考；现版采用宣纸、松墨、朱砂、古金的视觉系统，并加入地形纹理、符印角色、CD 圆环、战斗横幅和分享预览图。

## 已完成的工程内容

- 技术栈：React + Phaser 3 + TypeScript + Vite + IndexedDB + PWA。
- 规则集中在纯 TypeScript `GameCore`：Phaser 和 React 只负责渲染、反馈和派发命令。
- 战斗：连续时间 CD 自动战斗；攻速驱动普攻，秘术独立 CD，技能按优先级和槽位选择；玩家只操作三个丹药快捷槽。
- 地图：S/M/L 三档，16×16 / 24×24 / 32×32，1/2/3 层，seed 生成、传送门数量和关键连通性验证。
- 循环：洞府 → 选图 → 移动/采集 → 自动战斗 → 回府入库；战死/寿元耗尽 → 轮回 → 天赋 → 新一世。
- 洞府：仓库、采矿、炼丹、炼器、装配、三丹药快捷槽。
- 存档：IndexedDB 当前档 + 上一份成功档、损坏回退、JSON 导入导出、清档二次确认。
- PWA：manifest、standalone、Service Worker、离线静态壳、更新提示、横屏暂停。
- 视觉：`src/styles.css` 已完成首版精修；`public/og.jpg` 是 1200×630 公开分享图，源图保留在 `docs/og-source.png`。

## 关键文件入口

| 文件 | 作用 |
|---|---|
| `src/game/types.ts` | GameState、GameCommand、CombatState、SaveEnvelope 等契约 |
| `src/game/content.ts` | 地图档位、敌人、物品、秘术、配方、轮回天赋 |
| `src/game/core.ts` | 纯 reducer 与战斗/地图/轮回规则 |
| `src/game/mapGenerator.ts` | seeded 地图生成、连通性验证、视野揭示 |
| `src/game/save.ts` | IndexedDB、导入导出、版本校验 |
| `src/game/store.ts` | 状态订阅、命令派发、串行自动保存 |
| `src/components/MapView.tsx` | Phaser 地图、触控/滑动/WASD、Tween 和伤害反馈 |
| `src/App.tsx` | 洞府、选图、探索 HUD、丹药、弹窗、轮回界面 |
| `src/styles.css` | 响应式移动端视觉系统 |
| `tests/core.test.ts` | CD、MP 回退、先手、丹药、动画锁、死亡保留等核心测试 |
| `tests/mapGenerator.test.ts` | S/M/L 多 seed 地图验证 |
| `tests/save.test.ts` | 存档导入导出和版本拒绝 |
| `e2e/smoke.spec.ts` | 洞府到地图、设施和存档入口 |
| `e2e/pwa.spec.ts` | manifest、Service Worker、Chromium 离线刷新 |
| `.github/workflows/deploy-pages.yml` | GitHub Pages 自动构建和部署 |
| `vite.config.ts` | 根路径与 GitHub Pages 仓库子路径适配 |
| `vercel.json` | 可选的 Vercel 静态部署配置，暂不必删除 |

## 已验证证据

在工程目录执行：

```bash
npm run check
npm run test:e2e
```

当前结果：

- 核心测试：14/14 通过。
- 地图：S/M/L 各 100 个 seed，尺寸、层数、回府阵/深入门和连通性通过。
- 生产构建：通过；Phaser 被懒加载，主包和地图包仍有 Vite 大 chunk 提示，但不影响运行。
- 端到端：7 项通过，1 项跳过。
- 跳过项：Playwright WebKit 的离线网络模拟会触发内核错误；这不是游戏错误，真实 iPhone 15 离线冷启动必须实机验收。
- 人工窄屏：393×852 已跑通洞府 → S 图 → 移动 6 步 → 遭遇山狼 → CD 自动施法 → 胜利 → 刷新续玩。
- GitHub Pages 子路径构建：

```bash
GITHUB_REPOSITORY=CapooFun/ResignToCultivateWeb npm run build
```

manifest、Service Worker fallback、脚本、图标和分享图路径均已带 `/ResignToCultivateWeb/` 前缀。

## 当前未完成：真实 iPhone 15 验收

GitHub 授权与 Pages 发布已完成（见上）。工程已是 Git 仓库，`origin` = `git@github.com:CapooFun/ResignToCultivateWeb.git`，分支 `main`。

**请 Capoo 用 iPhone 15 打开** https://capoofun.github.io/ResignToCultivateWeb/ 并逐项勾：

1. Safari 竖屏：洞府 → 选小图 → 移动 → 遇敌自动战斗 → 回府
2. 分享 / 添加到主屏幕 → standalone 打开无地址栏
3. 刘海与底部 Home 条：底栏秘术 CD / 血蓝 / 三丹药不被遮挡
4. 锁屏或切后台再回前台：状态可续
5. 横屏提示/暂停是否符合预期
6. 飞行模式或断网后冷启动：离线壳是否仍能打开（交接注明 WebKit 离线 e2e 曾跳过，必须以实机为准）

通过后把结果回写本文件与 Vault `辞职修仙传_总览`；未通过则记具体机型/系统版本与复现步骤。

### 已完成的发布步骤（备查）

1. ~~用户创建仓库~~ → AI 用 `gh` 创建公开空仓 `CapooFun/ResignToCultivateWeb`
2. ~~`gh auth login`~~ → 本机 `CapooFun` token 有效（repo 权限）
3. `git init` + 首提 `feat: 发布辞职修仙传网页版首版` + push `main`
4. Pages Source = GitHub Actions（API `build_type=workflow`）
5. Actions 部署成功；公开 URL 可用

GitHub Pages 路线适合当前无后端、无账号、无分析 SDK 的静态 PWA；如果未来要做在线存档、排行榜、联机或 API，再重新评估 Vercel/后端服务。

## 接手时的安全边界

- 不读取、不提交 Vault 私人数据；玩法权威规格在 `/Users/Capoo/Infans_Vault/30_事业顺利/游戏开发/辞职修仙传/`。
- 不把 `node_modules/`、`dist/`、`test-results/` 或任何凭据提交到 GitHub。
- 不删除现有测试或为了通过测试改成伪造断言。
- 真实 iPhone 15 验收前，不把项目标记为完全公开验收完成。
- GitHub push 属于外部写入；确认仓库、分支和本次文件范围后再提交。

## 给下一位 AI 的最短任务提示

公开站已上线：https://capoofun.github.io/ResignToCultivateWeb/  
下一优先：**协助 Capoo 完成真实 iPhone 15 验收清单**（见上文），或根据实机反馈修 bug。不要重做已完成的玩法、视觉和测试。改代码后推 `main` 即触发 Pages 再发布。
